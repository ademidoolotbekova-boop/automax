# 🚀 Деплой на Render.com

## Шаг 1: Зарегистрируйтесь на Render

1. Перейдите на https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub аккаунт

## Шаг 2: Создайте новое приложение

1. В Dashboard нажмите "New +"
2. Выберите "Blueprint"
3. Подключите ваш GitHub репозиторий: `ademidoolotbekova-boop/automax`
4. Render автоматически найдет файл `render.yaml`

## Шаг 3: Настройте переменные окружения

Render покажет предпросмотр сервисов. Перед деплоем добавьте:

**RAILS_MASTER_KEY:**
```
2914fc0ca1b298758f03ec4ebcf72e8a
```

Нажмите на сервис `automax-web` → Environment → Add Environment Variable:
- Key: `RAILS_MASTER_KEY`
- Value: `2914fc0ca1b298758f03ec4ebcf72e8a`

## Шаг 4: Запустите деплой

1. Нажмите "Apply" для создания сервисов
2. Render автоматически:
   - Создаст PostgreSQL базу данных
   - Установит зависимости
   - Скомпилирует assets
   - Запустит миграции
   - Загрузит seed данные

## Шаг 5: Получите публичную ссылку

После успешного деплоя (5-10 минут):
- Ваше приложение будет доступно по адресу: `https://automax-web.onrender.com`
- Или ваш кастомный URL в настройках

## 📊 Что включено:

✅ Rails 8.0 + React + TypeScript
✅ PostgreSQL база данных
✅ 18 уроков на русском и английском
✅ 6 тестов с вопросами
✅ AI ассистент
✅ Полный билингвальный контент

## 🔑 Тестовый аккаунт:

- Email: `admin@example.com`
- Пароль: `password123`

## ⚠️ Важно:

- Free план Render: база данных удаляется через 90 дней
- Приложение засыпает после 15 минут бездействия
- Первая загрузка может занять 30-60 секунд (cold start)

## 🔧 Обновление приложения:

После изменений в коде:
```bash
git add .
git commit -m "Update"
git push origin main
```

Render автоматически задеплоит новую версию!
